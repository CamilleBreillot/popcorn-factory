<h4>Add your review</h4>
<%= simple_form_for([@list, @review ]) do |f| %>
          <%= f.input :comment %>
          <%= f.input :rating %>
          <%= f.submit "Create review", class:"btn-ghost" %>
<% end %>
